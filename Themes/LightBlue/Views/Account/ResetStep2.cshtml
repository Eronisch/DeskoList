@using Localization.Languages.Views.Account
@using Localization.Languages.Views.Shared
@model Topsite.Models.ResetStepTwoModel


@if (!ViewData.ModelState.IsValid)
{
    <div class='alert alert-danger'>
        <h4>@Global.Error</h4>
        @Html.ValidationSummary()
    </div>
}

<div class="bar">
    <div class="topMenu"><h1>@ResetStep2.MenuName</h1></div>
    <div class="content">
        <p class="nospace">@ResetStep2.Description</p>

        @using (Html.BeginForm("ResetStep2", "Account"))
        {
            @Html.HiddenFor(m=> m.Username)
            
            @Html.AntiForgeryToken()
            
            @Html.Label(Model.Question)

            @Html.TextBoxFor(m => m.Answer)

            <input type="submit" value="@ResetStep2.ResetPassword" class="btn btn-default" />
        }
    </div>
</div>